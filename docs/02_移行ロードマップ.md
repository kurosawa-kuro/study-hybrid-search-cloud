# 02 移行ロードマップ — 修正作業計画 (Recovery Plan)

参考実装 `/home/ubuntu/repos/study-gcp-mlops/2/study-ml-llm-reranking` から本リポジトリ `/home/ubuntu/repos/study-gcp-mlops/3/study-gcp-mlops-bq-first` への移植で **ハイブリッド検索の lexical 系統 (Meilisearch 相当) とキャッシュ層 (Redis 相当) が丸ごと欠落した** 問題の是正計画。

本書は `docs/README.md §2` の権威順位で最上位。`01_仕様と設計.md` / `03_実装カタログ.md` / `04_運用.md` / `CLAUDE.md` / `README.md` は本書の決定に従属する。本書が更新されたら連動ドキュメントも**同一 PR** で直す。

---

## 1. 問題認識 (なぜ「大失敗」か)

### 1.1 ハイブリッド検索の定義と実装の乖離

「ハイブリッド検索」は **dense (semantic / ベクトル) + sparse (lexical / キーワード BM25)** の 2 系統を融合する方式。参考実装では:

| 系統 | 参考実装のコンポーネント | 役割 |
|---|---|---|
| Sparse / lexical | **Meilisearch** (`src/clients/meilisearch_client.py`) | BM25 + typo tolerance + filterable attributes。`/search` の一次候補抽出 |
| Dense / semantic | **ME5 embeddings** + PostgreSQL `property_embeddings` + `similarity_service.py` | クエリ埋め込み × 物件埋め込みのコサイン類似度 |
| Fusion | `lgbm_trainer.py` + `/search` rerank | 9 次元特徴量 (lexical_rank / me5_score 含む) で LambdaRank |

本リポジトリの `BigQueryCandidateRetriever` (`app/src/app/adapters/candidate_retriever.py`) は **VECTOR_SEARCH 一本 (= dense のみ)** であり、`lexical_rank` という変数名は使っているが**実態は cosine 距離の順位**。9 次元特徴量のうち lexical_rank と me5_score は**同じ信号の別名**になっており、特徴量として独立していない。

### 1.2 キャッシュ層の消失

参考実装の `redis_client.py` は:

- SHA256(normalized params) をキーに `/search` 結果を TTL 120s でキャッシュ
- Redis 接続失敗を warn ログで飲み込み、呼び出し側は常に動く (graceful degradation)
- 負荷試験時の BigQuery スロット消費を抑える load-bearing な層

本リポジトリには**キャッシュ層が一切ない**。毎リクエスト VECTOR_SEARCH が走る。

### 1.3 ドキュメントと実装の整合も破綻

- `docs/01_仕様と設計.md:147-148` と `CLAUDE.md:12` で「**Meilisearch / Redis 不採用**」と宣言
- しかし `README.md:3` と `CLAUDE.md` 先頭は「**不動産ハイブリッド検索**」と呼称
- → lexical 系統を消した結果、名前と実装が矛盾。`01_仕様と設計.md:73-77` の「候補抽出層」の記述も semantic 単系統を「ハイブリッド」と呼んでおり誤り

### 1.4 失敗の根本原因

移植時に「**Meilisearch を BQ VECTOR_SEARCH で差し替え**」と短絡した判断 (旧 `02_移行ロードマップ.md` の差し替え表) が根本原因。正しくは:

- Meilisearch (lexical, BM25) → **BQ `SEARCH()` + `SEARCH INDEX`** など lexical の代替を別途用意
- ME5 vector search → **BQ `VECTOR_SEARCH` + `VECTOR INDEX`** で置換

二つの異なる役割を一つのコンポーネントに潰してしまった。

---

## 2. 是正の基本方針

### 2.1 ハイブリッド検索の 3-stage を正しく立て直す

```
Stage 1a (lexical):   Meilisearch で title/description/layout の BM25 → top 100 (lex)
Stage 1b (semantic):  BQ VECTOR_SEARCH で ME5 embedding cosine → top 100 (sem)
Stage 2  (fuse):      RRF (Reciprocal Rank Fusion) で 1a + 1b を融合 → 候補 100 件
Stage 3  (rerank):    LightGBM LambdaRank (Phase 6+) で 10 次元特徴量を使った再ランク → top 20
```

- 1a は Meilisearch REST API 経由 (property_id のみ返す)、1b は BQ VECTOR_SEARCH、融合 (2) は `app.services.ranking` 内で Python 実装 (property_id の rank リスト 2 本を RRF で合算し、上位 100 を BQ に戻して features / properties_cleaned と JOIN)
- `Candidate` dataclass に **`lexical_rank`** (= 1a の順位) と **`semantic_rank`** (= 1b の順位) を両方持たせる。現状の `lexical_rank` の意味が変わるので call site を全て直す
- `me5_score` は cosine 類似度 (浮動小数) をそのまま保持 (特徴量として使用)

### 2.2 キャッシュ層を戻す (ただし GCP-native に)

Redis 相当のキャッシュ層を復活させる。ただし **Memorystore for Redis を採用するか、別方式にするか** は選択肢 (§3.2) に分けて User 判断を仰ぐ。

### 2.3 ドキュメント側の決定を巻き戻す

`docs/01_仕様と設計.md` と `CLAUDE.md` と `README.md` の「Meilisearch 不採用 / Redis 不採用」宣言は、**正確には「Meilisearch コンテナと Redis コンテナの持ち込み不採用」であり、lexical 検索とキャッシュ層そのものは採用する**、と表現し直す。連動ドキュメントは同一 PR で直す (`docs/README.md §3`)。

---

## 3. 選択肢と User 判断待ち事項 (決定ポイント)

実装に入る前に User が決めるべき点を明示する。**これらが決まるまで実装フェーズに進まない**。

### 3.1 Lexical 検索の実装方式

| 選択肢 | 方式 | メリット | デメリット |
|---|---|---|---|
| **A** | BQ `SEARCH()` + `CREATE SEARCH INDEX` on `properties_cleaned(description, title, layout)` | GCP-native 維持 / 追加インフラなし / SQL 一本で VECTOR_SEARCH と同居可能 / IAM も同じ | BM25 ランキングが Meilisearch ほど洗練されていない / typo tolerance は弱い |
| **B** | Meilisearch を Cloud Run sidecar (同一サービス内 multi-container) として復活 | 参考実装と 1:1 / BM25 + typo tolerance が強力 | Cloud Run multi-container は Preview / 永続ボリューム設計必要 / index 同期ジョブが追加で必要 / `Meilisearch 不採用` の決定を巻き戻す |
| **C** | **A + B ハイブリッド**: Phase 5 までは A、本番精度が不足したら B に昇格可能な Port 分離 | 段階的 / 今は軽量 / 後戻り可能 | Port/Adapter 設計コストがわずかに増える |

**決定 (確定版): B** (2026-04-20 User 合意、当初 C の暫定決定から差し替え)。理由: Phase 2 (Local) と Phase 3 (GCP) で**検索スタックを揃える**ことで、親 `README.md` の「Phase 2 → 3 の学習ポイント (Local → Cloud 移植で何が変わるか)」が本来の切り分け (環境の違い / IaC・CI/CD・監視・コスト管理) に集中できる。BQ `SEARCH()` と Meilisearch BM25 のランキング差分を Phase 3 で別途検証する負荷を回避する。

- **Adapter 実装**: `MeilisearchLexical` (B)。Phase R3 で実装
- **非負制約の改訂**: 「BQ + Cloud Run のみ」→ 「BQ + Cloud Run + Meilisearch (on Cloud Run 2nd-gen multi-container、または別 Cloud Run Service)」に緩和。`CLAUDE.md` の非負制約表を Phase R1 で書き換える
- **Port 分離は維持**: `LexicalSearchPort` は残す。将来 BQ `SEARCH()` に差し替える選択肢は Port レベルで開けておく (A は future option)

### 3.2 キャッシュ層の実装方式

| 選択肢 | 方式 | メリット | デメリット |
|---|---|---|---|
| **a** | **Memorystore for Redis (Standard, 1GB)** | 参考実装とコード互換 / 永続 / Cloud Run から VPC connector 経由で接続 | 月 ~$35 / VPC connector も必要 ($10) / Redis 不採用の決定を巻き戻す |
| **b** | **Cloud Run Service の in-memory TTL キャッシュ (`cachetools.TTLCache`)** | 追加インフラゼロ / `min-instances=1` なら warm / 「同一クエリが短時間に繰り返される」パターンで実用ヒット率 | instance 間でキャッシュ共有されない / 再デプロイで消える |
| **c** | **BigQuery の query cache に任せる** (=何もしない) | ゼロ / BQ は同一クエリ 24h キャッシュする | クエリ埋め込み (FLOAT64 ARRAY 768d) が毎回微妙に違うと効かない / 実質的に効かない |

**決定: b** (2026-04-20 User 合意)。素の `functools.lru_cache` は TTL 非対応のため **`cachetools.TTLCache`** を採用 (`pyproject.toml` の `app` workspace に追加)。検索クエリは「同一クエリが短時間に繰り返される」パターンで効き、`min-instances=1` + 単一インスタンス運用下で実用ヒット率が出る前提。`CacheStore` Port で抽象化するため、将来トラフィックに応じて a (Memorystore) へ昇格可能。

### 3.3 RRF (Reciprocal Rank Fusion) の k パラメータと融合位置

- `k = 60` が文献デフォルト (Cormack et al.)。本リポジトリは `k = 60` を開始点とし、`app/src/app/services/ranking.py` に定数化
- 融合は **Cloud Run Service 内の Python で実行**。理由: lexical (Meilisearch) と semantic (BQ) が別システムになったため、どちらかに寄せると残りをそのシステムに引き込む必要があり現実的でない。融合対象は `property_id` の rank リスト 2 本だけ (各 top 200 = 計 400 行) なので Python 処理で十分軽い
- 融合後の top 100 を BQ に IN 句で戻して `properties_cleaned` / `property_features_daily` を JOIN 取得し、`Candidate` dataclass を組み立てる

### 3.4 特徴量の再定義 (9 次元 → 10 次元)

現状 9 次元 (`FEATURE_COLS_RANKER`) のうち `lexical_rank` は semantic rank の別名になっていた。正しい 10 次元に拡張する:

| # | カラム | 出所 | 訓練 / 推論 |
|---|---|---|---|
| 1 | `rent` | properties_cleaned | 両方 |
| 2 | `walk_min` | properties_cleaned | 両方 |
| 3 | `age_years` | properties_cleaned | 両方 |
| 4 | `area_m2` | properties_cleaned | 両方 |
| 5 | `ctr` | property_features_daily | 両方 |
| 6 | `fav_rate` | property_features_daily | 両方 |
| 7 | `inquiry_rate` | property_features_daily | 両方 |
| 8 | `me5_score` | クエリ時 (VECTOR_SEARCH cosine) | 推論 / 訓練はログ |
| 9 | `lexical_rank` | **クエリ時 (SEARCH() の BM25 順位)** — 再定義 | 推論 / 訓練はログ |
| 10 | `semantic_rank` | クエリ時 (VECTOR_SEARCH の順位) | 推論 / 訓練はログ |

訓練データは `mlops.ranking_log` に `lexical_rank` / `semantic_rank` / `me5_score` を全部残すため、log record のスキーマ拡張が必須 (§4.2 で詳述)。`lexical_rank` の「出所」が Meilisearch BM25 スコアの順位であることを Phase R3 commit で明記する。

### 3.5 Meilisearch / Redis 「不採用」宣言の撤回

現行ドキュメント (`01_仕様と設計.md:147-148` / `CLAUDE.md` / `README.md`) の「Meilisearch 不採用」「Redis 不採用」は **設計ミス**。過去の意図を汲んで言い換えるようなものではなく、誤った決定として撤回する。

- 誤り 1: lexical 検索を実装しないまま「ハイブリッド検索」と銘打っていた
- 誤り 2: キャッシュ層を最初から放棄し、BQ スロット消費を無対策にしていた
- 撤回理由: ハイブリッド検索の定義 (§1.1) と参考実装の設計意図 (§7) に照らして、lexical stage とキャッシュ層は両方とも load-bearing

修正後の宣言:

- ✅ **採用**: lexical 検索 (**Meilisearch v1.7+ を Cloud Run に乗せる**、Port 経由で将来 BQ `SEARCH()` に差し替え可) / オンラインキャッシュ (`cachetools.TTLCache`)
- ❌ **不採用**: Redis サーバ (コンテナ or Memorystore、将来必要なら §3.2 a に昇格) / PostgreSQL / Vertex AI / BQML
- ⚠️ **条件付き採用**: Meilisearch は「サーバコンテナとして GCP に載せる」。参考実装の `getmeili/meilisearch:v1.7` image を Artifact Registry に mirror し、Cloud Run Service としてデプロイする (sidecar vs 独立 Service の選択は Phase R3 実装時に §3.6 で決定)

本節は「表現の修正」ではなく「決定の巻き戻し」。Phase R1 のドキュメント整合 PR で `01 / CLAUDE / README` から「Meilisearch 不採用 / Redis 不採用」の単独列挙を削除し、本書 §3.5 の決定文で置き換える。

### 3.6 Meilisearch の GCP 実装形態

**決定: α (独立 Cloud Run Service + GCS FUSE mount)** (2026-04-20 User 合意、確定)。

判断根拠: 本リポジトリは**学習検証用であり実務運用ではない** (親 `README.md` Phase 3 は「不動産ハイブリッド検索(実務)」と題しているが、実態は Cloud Native 化の学習題材)。したがって α のデメリット (cold start で GCS FUSE から index ロード / マルチインスタンス時の一貫性課題) は許容範囲。β (sidecar) / γ (GCE VM) の複雑さは学習目的に対して過剰。

比較表 (参考):

| 方式 | 内容 | メリット | デメリット |
|---|---|---|---|
| **α (採用)** | **独立 Cloud Run Service** (`meili-search`)。search-api から IAM 認証付き HTTP で呼ぶ | シンプル / IaC 差分が小さい / Cloud Run 料金のみ / Meilisearch が落ちても search-api は動く (noop fallback) | 永続ストレージは **GCS FUSE mount** (index を `gs://mlops-dev-a-meili-data/` に置く) / cold start で index ロードが走る / 複数インスタンスでの GCS 書込競合は `meili-sync-job` 1 本に書込を限定して回避 |
| β | search-api と同一 Cloud Run Service の sidecar container | ネットワーク遅延ゼロ | sidecar の ライフサイクル管理 / search-api との blast radius 結合 |
| γ | GCE VM + Meilisearch + 永続ディスク | 永続化が自明 | 「Cloud Run only」から逸脱 / VM パッチ管理 |

**具体構成** (Phase R3 実装時の前提):

- Cloud Run Service 名: `meili-search`
- イメージ: Artifact Registry に mirror した `getmeili/meilisearch:v1.7`
- Cloud Run 設定: `--cpu 1 --memory 2Gi --min-instances 1 --max-instances 2 --no-allow-unauthenticated --execution-environment gen2`
- GCS FUSE mount: `gs://mlops-dev-a-meili-data` → `/meili_data` (環境変数 `MEILI_DB_PATH=/meili_data`)
- SA: `sa-meili` (新設、`storage.objectAdmin` on `mlops-dev-a-meili-data` のみ)
- search-api → meili-search の認証: `sa-api` に `roles/run.invoker` on `meili-search` を付与、search-api 側は `google.auth.transport.requests` で ID token を取得して Authorization header に載せる

Meilisearch index 同期は **Cloud Run Jobs `meili-sync-job`** を新設 (参考実装の `src/jobs/sync_properties.py` 相当を GCP 版に移植)。Dataform 完走後の Cloud Scheduler trigger で日次走らせる (04:30 JST 想定)。

---

## 4. 具体的な修正作業 (Phase 別)

User が §3 を決定した後、以下の順で実装する。各 Phase は 1 PR で完結させる。

### Phase R1 — ドキュメント整合 (コード無改修)

目的: 仕様側を先に直し、実装作業の着地点を明確にする。

1. `docs/02_移行ロードマップ.md` (本書) を merge
2. `docs/01_仕様と設計.md` — §① の「Vertex AI / Meilisearch / Redis / PostgreSQL / BQML 不採用」を「Meilisearch サーバ / Redis サーバ / PostgreSQL / Vertex AI / BQML 不採用」に差し替え、候補抽出層の説明を 3-stage (lex + sem + fuse) に書き直す
3. `CLAUDE.md` — 「bq-first の設計テーゼ」から「Meilisearch 不採用」の単独列挙を外し、3-stage を `(1a) Meilisearch lexical / (1b) BQ VECTOR_SEARCH semantic / (2) RRF fusion / (3) LambdaRank` として再記述。非負制約表に `lexical_retrieval_method` を追加
4. `README.md` — 冒頭の宣言を同じ方針で直す
5. `docs/03_実装カタログ.md` — §候補抽出の項に `SEARCH INDEX` / RRF SQL の項を追加 (実装は後 Phase)
6. `docs/04_運用.md` — STEP 16 (VECTOR INDEX 手動 DDL) の後に **STEP 16b: `CREATE SEARCH INDEX`** を追加

**受け入れ基準**: `grep -rn "Meilisearch" docs/ CLAUDE.md README.md` と `grep -rn "Redis" docs/ CLAUDE.md README.md` の全ヒットが「サーバとしては不採用」もしくは「参考実装の I/O 層」の文脈になっている。

### Phase R2 — Port / Adapter の再設計 (インフラ無変更)

目的: 実装を入れる前に境界を整える。Port を変えるので layer 境界テスト (`tests/arch/test_import_boundaries.py`) が先に FAIL するよう赤で書き始める。

1. `common/src/common/schema/feature_schema.py` — `FEATURE_COLS_RANKER` を 9 → 10 列に拡張 (`lexical_rank` の意味が変わり、`semantic_rank` を新規追加)
2. `app/src/app/ports/candidate_retriever.py` — `Candidate` dataclass に `semantic_rank: int` を追加 / `lexical_rank` の docstring を「BM25 順位」と明記
3. `app/src/app/ports/cache_store.py` — 新規。`CacheStore` Protocol を定義 (`get(key) -> dict | None` / `set(key, value, ttl) -> None`)
4. `app/src/app/adapters/cache_store.py` — 新規。`NoopCacheStore` / `InMemoryTTLCacheStore` (`cachetools.TTLCache` ベース、§3.2.b)。`MemorystoreRedisCacheStore` (§3.2.a) は将来の昇格用に Port 適合スタブのみ用意しても良いが、Phase R4 では実装しない
5. `app/src/app/services/ranking.py` — RRF 融合ロジックをここに置くか、BQ 側に置くかは §3.3 通り BQ 側。本ファイルには RRF の `k` 定数と cache key 正規化関数のみ
6. `tests/arch/test_import_boundaries.py` に `app.ports.cache_store` / `app.adapters.cache_store` ルールを追加

**受け入れ基準**: `make check` および `make check-layers` が PASS。機能は未実装なので `make api-dev` で `/search` が古い挙動のまま動く。

### Phase R3 — Lexical 候補抽出の実装 (§3.1 決定 B / §3.6 で方式確定)

1. **Meilisearch サーバ配置** (§3.6 α 想定):
   - `infra/modules/meilisearch/` 新設: Cloud Run Service `meili-search`、イメージは Artifact Registry に mirror した `getmeili/meilisearch:v1.7`、GCS バケット `mlops-dev-a-meili-data` を Cloud Storage FUSE で `/meili_data` にマウント、`--no-allow-unauthenticated` で IAM gate、SA `sa-meili` 新設
   - `CLAUDE.md` 非負制約表に `meili-search` Cloud Run 設定 (`--cpu 1 --memory 2Gi --min-instances 1 --max-instances 2`) を追記
2. **index 同期 Job** `meili-sync-job` 新設:
   - `jobs/src/meili_sync/entrypoints/sync_cli.py` 新規。`feature_mart.properties_cleaned` から `property_id / title / description / layout / rent / walk_min / age_years / pet_ok` を読み Meilisearch REST API に PUT
   - `filterable_attributes`: `rent / walk_min / age_years / layout / pet_ok`
   - `searchable_attributes`: `title / description / layout`
   - Cloud Scheduler で Dataform 完走後 (04:30 JST 想定) に trigger
3. **Lexical Adapter 実装** (`app/src/app/adapters/lexical_search.py` 新規):
   - `MeilisearchLexical` class。`LexicalSearchPort` に適合
   - `search(query: str, filters: dict, top_k: int) -> list[tuple[property_id, lexical_rank]]`
   - 参考実装 `src/clients/meilisearch_client.py` の `search()` を流用、httpx で Meilisearch `/indexes/properties/search` に POST。filter 式は `rent <= {max_rent} AND layout = "{layout}" ...` 形式に組み立て
4. **BigQueryCandidateRetriever.retrieve()** を書き換え (`app/src/app/adapters/candidate_retriever.py:58-145`) — **3-stage fusion を Python 側で実装**:
   ```python
   def retrieve(self, *, query_text, query_vector, filters, top_k):
       # Stage 1a: Meilisearch BM25
       lex_results = self._lexical.search(query=query_text, filters=filters, top_k=200)
       # Stage 1b: BQ VECTOR_SEARCH (filter 込み top 200)
       sem_results = self._vector_search(query_vector, filters, top_k=200)
       # Stage 2: RRF fusion
       fused_ids = rrf_fuse(lex_results, sem_results, k=60, top_n=100)
       # Stage 2.5: BQ に戻して features / properties 属性 JOIN 取得
       return self._enrich_from_bq(fused_ids, lex_results, sem_results)
   ```
5. **Retriever の DI 変更**: `BigQueryCandidateRetriever.__init__` に `lexical: LexicalSearchPort` を追加。composition root (`app/src/app/entrypoints/api.py` の lifespan) で `MeilisearchLexical` を注入
6. **推論側 Python** (`common/src/common/feature_engineering.py::build_ranker_features`) に `semantic_rank` を追加し、9 列 → 10 列で特徴量ベクトルを組む
7. **Pub/Sub payload** (`app/src/app/adapters/candidate_retriever.py:176-186`) の `features` RECORD に `semantic_rank` を追加
8. **Infra schema** (`infra/modules/data/main.tf` の `ranking_log.features` RECORD) に `semantic_rank FLOAT64 NULLABLE` を追加

**受け入れ基準**: `make check` PASS + `tests/parity/test_feature_parity_ranking.py` が 10 列で PASS + `make ops-search` で実環境に `meili-search` 経由の BM25 + BQ VECTOR_SEARCH の RRF 融合結果が返る + `meili-sync-job` の 1 回実行後に Meilisearch index に properties_cleaned 行数と同じドキュメント数が入る。

### Phase R4 — キャッシュ層の配線 (§3.2 で b を選んだ場合)

1. `app/src/app/adapters/cache_store.py::InMemoryTTLCacheStore` を実装 (**`cachetools.TTLCache` を使用**、`pyproject.toml` の `app` workspace の runtime 依存に `cachetools>=5.3` を追加)
2. `app/src/app/entrypoints/api.py` の `lifespan` で cache store をインスタンス化、DI に流す
3. `/search` エンドポイントで `cache.get(key) or (retrieve → cache.set(key, result))`
4. cache key: SHA256 of JSON-normalized `{query, filters, top_k}` (参考: `redis_client.py:34-37`)
5. TTL 環境変数: `SEARCH_CACHE_TTL_SECONDS` (既定 120、非負制約表に追加)
6. 障害時 graceful degradation: `NoopCacheStore` に fallback (例外を飲み込む責務は Adapter 側)

**受け入れ基準**: `tests/services/test_ranking.py` に cache hit / miss / TTL 経過 / 障害時 fallback の 4 ケース追加、全 PASS。

### Phase R5 — 参考実装の `ranking_compare_logs` 相当の追加

参考実装では「Meili 順」と「rerank 順」を並べてログに残し (`ranking_compare_repository.py`)、精度低下を発見するシグナルにしていた。本リポジトリでは Phase 6 (LambdaRank 本番連携) までブロックだが、**Phase R3 の時点で `lexical_rank` / `semantic_rank` / `rrf_rank` の 3 本をログに残す** ことで同等の比較基盤を先取りする。

1. `ranking_log.features` に `rrf_rank INT64 NULLABLE` を追加 (Phase R3 の拡張と同時)
2. Scheduled Query `ranking_rank_diff_daily` を新設 (`definitions/monitoring/ranking_rank_diff_daily.sqlx`): 日次で `CORR(lexical_rank, semantic_rank)` を吐き、極端に小さいと警告

---

## 5. ドキュメント連動表 (各 Phase で同時に直すファイル)

| Phase | コード | Dataform | Terraform | Tests | Docs |
|---|---|---|---|---|---|
| R1 | — | — | — | — | `01 / 02 / 03 / 04 / CLAUDE / README` |
| R2 | `app/ports/*` / `app/adapters/cache_store.py` / `common/schema/feature_schema.py` | — | — | `tests/arch/*` | `03_実装カタログ.md` |
| R3 | `app/adapters/candidate_retriever.py` (DI 変更) / `app/adapters/lexical_search.py` (新規) / `common/feature_engineering.py` / `jobs/src/meili_sync/**` (新規) | `property_features_daily.sqlx` の変更は**不要** (訓練側の特徴量計算式は property-side 7 列で変わらない) | `infra/modules/meilisearch/` (新規: Cloud Run Service + GCS FUSE + SA `sa-meili`) / `infra/modules/runtime/main.tf` に `meili-sync-job` 追加 / `ranking_log.features` に `semantic_rank` / `rrf_rank` 追加 | `tests/parity/test_feature_parity_ranking.py` / `tests/parity/test_feature_parity_sql_ranker.py` / `tests/infra/test_meilisearch_module.py` (新規) | `03 実装カタログ §Meilisearch` 新設 / `04 運用` に Meili index ビルド手順を追加 (STEP 16 の隣) |
| R4 | `app/adapters/cache_store.py` / `app/entrypoints/api.py` | — | `infra/modules/runtime/main.tf` (環境変数 `SEARCH_CACHE_TTL_SECONDS`)、§3.2.a を選んだ場合のみ `infra/modules/cache/` 新設 | `tests/services/test_cache.py` 新規 | `03 / 04 / CLAUDE (非負制約)` |
| R5 | — | `definitions/monitoring/ranking_rank_diff_daily.sqlx` | `ranking_log.features` の RECORD 拡張 | `tests/parity/*` | `04_運用 §2` に監視クエリを追加 |

---

## 6. Feature parity invariant の再定義 (旧 5 ファイル → 新 5 ファイル)

`CLAUDE.md` の feature parity invariant を以下に差し替え:

> 特徴量 (property-side 7 列: `rent` / `walk_min` / `age_years` / `area_m2` / `ctr` / `fav_rate` / `inquiry_rate`) を追加 / 変更するときに揃える 5 ファイルは変わらず:
>
> 1. `definitions/features/property_features_daily.sqlx`
> 2. `common/src/common/feature_engineering.py::build_ranker_features`
> 3. `common/src/common/schema/feature_schema.py::FEATURE_COLS_RANKER`
> 4. `infra/modules/data/main.tf` の `ranking_log.features` RECORD
> 5. `monitoring/validate_feature_skew.sql` の UNPIVOT
>
> **query-side 3 列 (`me5_score` / `lexical_rank` / `semantic_rank`) は property-side の skew 監視の対象外**。代わりに Phase R5 で新設する `ranking_rank_diff_daily` で lexical と semantic の相関を監視する。

---

## 7. 参考実装との対応表 (is / was / will be)

| 参考実装 (study-ml-llm-reranking) | 旧 bq-first (誤った移植) | 修正後の bq-first |
|---|---|---|
| PostgreSQL `properties` | BQ `feature_mart.properties_cleaned` | 同左 (変更なし) |
| PostgreSQL `search_logs` / `property_features` / `property_embeddings` | BQ `mlops.ranking_log` / `feature_mart.property_features_daily` / `feature_mart.property_embeddings` | 同左 (変更なし) |
| Meilisearch `properties` index (BM25 lexical) | ❌ 欠落 (VECTOR_SEARCH を誤って `lexical_rank` と呼んでいた) | ✅ **Meilisearch v1.7 on Cloud Run** (§3.1 B、Port 経由で将来 BQ `SEARCH()` 差し替え可) — Phase 2 と検索スタックを揃える決定 |
| ME5 `property_embeddings` (dense vector) | BQ `VECTOR_SEARCH` | 同左 (名前は `semantic_rank` に修正) |
| Redis cache (TTL 120s, graceful fallback) | ❌ 欠落 | ✅ **`CacheStore` Port 経由で in-memory LRU (Phase R4)**、将来 Memorystore 昇格可 |
| Meili 順 vs rerank 順 比較ログ (`ranking_compare_logs`) | ❌ 欠落 | ✅ `mlops.ranking_log` に 3 系統の rank を同居させ Scheduled Query で相関監視 (Phase R5) |
| LightGBM LambdaRank (`lgbm_trainer.py`) | `jobs/src/training/services/rank_trainer.py` | 同左 (変更なし。特徴量だけ 9 → 10 列に拡張) |
| Docker Compose (api + postgres + meilisearch + redis) | Cloud Run Service + Cloud Run Jobs + BigQuery + Pub/Sub | 同左 (I/O は BQ に集約の方針は維持) |

**原則 (参考実装からの継承)**: 純粋ロジック (ranking metrics / label gain / feature 計算式 / LambdaRank ハイパラ) は可能な限りそのまま流用する。I/O 層は本リポジトリの Port/Adapter 構造に吸収する。**ただしロジックの要である "lexical + semantic の 2 系統" は絶対に潰さない**。

---

## 8. リスクと緩和策

| リスク | 緩和策 |
|---|---|
| Meilisearch の cold start で GCS FUSE からの index ロードが遅い | `min-instances=1` を初期設定。index サイズが MB オーダなら許容範囲。問題が出たら §3.6 β (sidecar) に切り替え |
| Meilisearch Cloud Run Service の永続化設計 (GCS FUSE の一貫性) | 書込は `meili-sync-job` 1 本に限定 / `meili-search` は読み取り専用運用 / 同時更新なしの前提で整合性リスクを下げる |
| Artifact Registry への Meilisearch image mirror が初期セットアップで必要 | `docs/04_運用.md` に STEP (Meilisearch image pull → retag → push) を追加。Terraform 外の手動 STEP |
| RRF の `k=60` がドメイン (不動産短文) に合わない | Phase 6 の NDCG@10 評価で validation set での k スイープ (k ∈ {10, 30, 60, 100}) を走らせる |
| `lexical_rank` の意味変更で既存 `mlops.ranking_log` 行が混在し訓練データが汚れる | ログスキーマ拡張時に `schema_version` カラムを追加し、訓練時に `WHERE schema_version >= 2` でフィルタ |
| Cloud Run in-memory cache が instance 間で不整合 | MVP 段階では許容。`max-instances` を一時 3 以下に絞り、p95 レイテンシで影響を見て必要なら §3.2 a に昇格 |
| Meilisearch / Redis 不採用決定の巻き戻しが過去コミット・PR 説明と矛盾 | 本書の §1.4 で経緯を明記し、`CLAUDE.md` の「ドキュメント衝突時の権威順位」に従って本書を正とする |

---

## 9. 作業キューと現状

| # | Phase | 内容 | 状態 |
|---|---|---|---|
| 0 | — | 本書 (修正作業計画) 初稿 | ✅ 作成済 |
| 1 | — | §3 の決定ポイント | ✅ **全決定済 (2026-04-20)**: §3.1=**B (Meilisearch on Cloud Run)** / §3.2=b (`cachetools.TTLCache`) / §3.5=撤回 / §3.6=**α (独立 Cloud Run Service + GCS FUSE、学習検証用として許容)** |
| 2 | R1 | ドキュメント整合 PR | ✅ 実装済 |
| 3 | R2 | Port/Adapter 再設計 + schema 10 列化 | ✅ 実装済 |
| 4 | R3 | Lexical 検索 (Meilisearch) 実装 + RRF 融合 | ✅ 実装済 |
| 5 | R4 | CacheStore 配線 | ✅ 実装済 |
| 6 | R5 | ranking_rank_diff 監視 SQL | ✅ 実装済 |

注記: 上記は「コード反映済」を示す。実行系の最終検証/負荷検証は別セッションで実施する。

---

## 10. User 判断ログ (合意済み)

2026-04-20 時点で以下 3 点が合意済み。Phase R1 以降はこの決定に基づいて着手する:

1. **§3.1 — Lexical 検索の方式**: **B** (Meilisearch v1.7 を Cloud Run に乗せる。Phase 2 Local と検索スタックを揃えることで Phase 2 → 3 の学習ポイントを「環境の違い / Cloud Native 化」に純化する)。`LexicalSearchPort` は残し、将来 BQ `SEARCH()` に差し替え可
2. **§3.2 — キャッシュ方式**: **b** (in-memory TTL キャッシュ、**`cachetools.TTLCache`** を使用。`functools.lru_cache` は TTL 非対応のため不採用)
3. **§3.5 — 表現**: 「Meilisearch 不採用 / Redis 不採用」は**設計ミスとして撤回**。単なる表現書き換えではない。Meilisearch は**条件付きで採用** (§3.5 参照)
4. **§3.6 — Meilisearch の GCP 実装形態**: **α (独立 Cloud Run Service + GCS FUSE)** 確定。本リポジトリが学習検証用であり、α のデメリット (cold start での index ロード / マルチインスタンス時の整合性) は許容範囲であるため β / γ は不採用

以上をもって本書は **合意済みの是正計画** とする。本書に記載のない技術 (Vertex AI / PostgreSQL / cross-encoder 等) を採用する変更、および本書の決定 (§3.1=B / §3.2=b / §3.5) を逸脱する変更は、本書の更新を伴わない限り **差し戻し対象**。
